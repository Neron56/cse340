<%- include('partials/header') %>
<main>
    <h1><%= title %></h1>
    <% categories.forEach(category => { %>
        <h2><%= category.name%></h2>
    <% }); %>
</main>
<%- include('partials/footer') %>